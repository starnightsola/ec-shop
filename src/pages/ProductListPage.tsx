import { Typography, Container } from '@mui/material'

const ProductListPage = () => {
  return (
    <Container>
      <Typography variant="h4">商品一覧ページ</Typography>
    </Container>
  )
}

export default ProductListPage